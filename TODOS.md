# Form Identity — TODOS

_Generated by /autoplan · 2026-05-07_

---

## v2 — Build Order

### Preflight (do first, unblocks everything)

- [ ] **Fix 3 bogus type codes** — rename `ORLD→OCRD`, `ORLF→OCRF`, `OARL→OARD` in:
  - `web/components/BrandCard.tsx` (BRAND_TYPES array + index array in result page)
  - `form-identity-system.json` (brand_types + colour_options keys)
  - `web/app/result/[code]/page.tsx` (the hardcoded index lookup array)
  - Any other string references (grep for `ORLD`, `ORLF`, `OARL`)
- [ ] **Encode quiz answers by question ID** — change `encodeAnswers` / `decodeAnswers` in `web/lib/quiz-state.ts` to use `q.id` (`E1`, `S3`, etc.) instead of array index. Fixes silent re-scoring when questions reorder.
- [ ] **Add `generateMetadata`** to `web/app/result/[code]/page.tsx` — minimum: type name as title, character line as description, `twitter:card: summary_large_image`.

### Bugs

- [ ] **B1 — Card footer** — `web/app/globals.css`: remove `margin-bottom: auto` from `.card-stats`, add `flex-shrink: 0` to `.card-footer`.
- [ ] **B2 — Quiz labels** — `web/components/Quiz.tsx` OPTIONS array: change `"Lean"` to `"Lean A"` and `"Lean B"`.

### Features

- [ ] **F1/F6 — `/types` gallery + individual type pages**
  - New route: `web/app/types/page.tsx` — 4×4 grid of all 16 BrandCards, each linking to `/types/[code]`
  - New route: `web/app/types/[code]/page.tsx` — static, `generateStaticParams` for all 16 valid codes, `dynamicParams = false`
  - Gallery: highlight "your type" if coming from result (pass code as query param)
  - Card responsive: `minmax(320px, 480px)` column grid on gallery; card scales to container on type page
  - "See all 16 types" link on result page below axis breakdown

- [ ] **F2 — Share**
  - Add `web/app/result/[code]/opengraph-image.tsx` using `ImageResponse` from `next/og`
  - OG image: static per-type (type name + character line + type color block). No per-user personalisation — `answers` param is not available to the OG image route.
  - **Use static-weight font files** (not variable TTF) inside ImageResponse — satori doesn't support variable font axes. Need to either subset Inter 300/400 or use system fallback.
  - OG image layout: flexbox only (no CSS grid — satori doesn't support it)
  - Share UI: text-only options (no platform logos). Inline panel on desktop, bottom drawer on mobile.
  - Options: "Copy link", "Share on X", "Share on LinkedIn", "Share via email"
  - Copy link: absolute URL using `window.location.href` inside click handler (not render)
  - Copy success state: label swaps to "Copied" for 2s
  - Clipboard failure fallback: show URL in a selectable `<input readonly>` field

- [ ] **F5 — Home page** (reduced scope)
  - One paragraph below the CTA fold: the diagnostic philosophy ("most branding starts with aesthetics; Form Identity starts with character")
  - EB Garamond italic, 16px, max 4 sentences
  - Below a 1px rule separator — quiz CTA stays above fold, full-width

- [ ] **Email capture opt-in** (replaces F3/F4 in v2 slot)
  - Single line at bottom of result page: "Email me my result →"
  - Simple email input + submit, no gating (result already visible)
  - Store in a lightweight backend (Resend, Loops, or even a Google Sheet webhook for now)
  - No authentication needed — just name + email + type code

### Code Quality

- [ ] **Consolidate axis constants** — `AXES`/`AXIS_POLES`/`AXIS_LABELS` are defined independently in BrandCard.tsx, Quiz.tsx, and result/page.tsx. Move canonical definition to `web/lib/brand-type-engine.ts`, import everywhere.
- [ ] **Add unit tests** (Jest or Vitest):
  - `scoreAxis` and `scoreBrandType` with known input/output pairs
  - `encodeAnswers` / `decodeAnswers` round-trip
  - Type code validity: every code in BRAND_TYPES matches `[OI][CA][LR][DF]`
  - `getBrandTypeData` returns non-null for all 16 valid codes

---

## v3 — Deferred

- **F3 — Colour suggestions** — blocked on content: need 15 more curated palettes (only ICLD exists). Schema in `web/lib/schema.ts` is ready; it's a data entry task.
- **F4 — Font pairing suggestions** — blocked on content: no per-type pairings exist. Add `FontPairingSchema` to `web/lib/schema.ts` and populate all 16 types.
- **PDF brand kit download** — full brand output; requires design work per type
- **Auth / saved results** — persistent result URLs (vs query-param encoding)
- **B2 full copy rewrite** — add `aDefinitely`/`aLean`/`bLean`/`bDefinitely` to all 24 questions (64–96 new strings). Content sprint, not a code task.

---

## Pre-existing risks

- **`generatePalette` contrast** — validate all 16 type hex values against warm-white (#F5F2EA) for WCAG AA. Warm saturated types (OCLD #C8633A, OARL #B85A2A) may be borderline.
- **Figma plugin** — `figma-plugin/` directory exists but intent/status is undefined. Decide: maintain, archive, or integrate.
- **`form-identity-system.json` scoring metadata** — `meta.scoring.range`, `midpoint`, `max_distance` reflect 4 questions/axis but the engine now uses 6. Docs only, no runtime effect, but misleading.
